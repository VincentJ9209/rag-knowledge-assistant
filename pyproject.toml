[project]
name = "rag-knowledge-assistant"
version = "0.1.0"
description = "A persistent RAG knowledge assistant with grounded generation, function calling, and an explicit agentic loop."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jupyter>=1.1.1",
    "minsearch>=0.2.0",
    "openai>=2.53.0",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
    "sqlitesearch>=0.3.0",
]
